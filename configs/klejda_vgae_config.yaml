in_channels: 3
max_nodes: 25
latent_dim: 16
learning_rate: 0.001

encoder_conv_channels:
  - 16
  - 32

encoder_dense_features:
  - 128
  - 64

decoder_a_hidden_dims:
  - 64
  - 128

decoder_x_conv_channels:
  - 32
  - 16

decoder_x_dense_features:
  - 64
  - 128

weight_a: 1000.0
kl_weight: 0.005

locality_loss_type: "fitness" # 'fitness', 'parts_num' , 'dissimilarity'
locality_loss_method: "pearson"
locality_loss_lambda_val: 1.0
save_dir: "./checkpoints"